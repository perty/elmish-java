package se.artcomputer.elmish;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Cmd {
    private static final Logger LOG = LoggerFactory.getLogger(Cmd.class);

    public static Cmd none = new Cmd();
}

